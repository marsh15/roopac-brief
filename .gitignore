# deps
/node_modules

# next
/.next
/out
next-env.d.ts

# crawl cache (raw HTML; committed JSON lives in data/)
/.cache

# env
.env
.env.*
!.env.example

# misc
.DS_Store
*.tsbuildinfo
.vercel

# runtime state (job counter)
/.runtime
